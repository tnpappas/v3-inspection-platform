# Raw API dumps may contain client PII. Never commit.
discovery/raw/
discovery/csv/

# Just in case
*.env
*.secret
secrets/

# Replit project snapshot (binary + extracted source). Not our code, do not commit.
replit-snapshot/
replit-snapshot.tar.gz
