/**
 * Barrel for @v3/permissions/catalog.
 * Re-exports the seed catalog (permissions, groups, group memberships, default
 * role permissions, implicit denies, CI test helpers) from permissions-seed.ts.
 */
export * from './permissions-seed.js';
