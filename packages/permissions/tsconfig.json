{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    // Inherits the same Drizzle circular-reference loosening as @v3/db since
    // we import schema.ts here for type extraction (Role enum, etc.).
    "noImplicitAny": false
  },
  "include": ["src/**/*"],
  "exclude": ["dist", "node_modules", "tests"]
}
